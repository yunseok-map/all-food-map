# 1. 기반 이미지 선택 (어떤 환경에서 시작할지 결정)
# 가장 가볍고 인기 있는 웹서버인 Nginx 최신 버전을 기반으로 합니다.
FROM nginx:alpine

# 2. 내 프로젝트 파일 복사
# 현재 폴더(.)의 모든 파일을 Nginx 웹서버의 기본 폴더(/usr/share/nginx/html)로 복사합니다.
# 이 폴더에 파일이 있어야 Nginx가 웹사이트를 보여줄 수 있습니다.
COPY . /usr/share/nginx/html

# 3. 포트 설정 (선택사항이지만 권장)
# 이 컨테이너는 80번 포트를 사용한다고 외부에 알려줍니다.
# Nginx의 기본 포트는 80입니다.
EXPOSE 80

# 4. 서버 실행 명령어 지정
# 이미지를 실행(컨테이너화)했을 때 Nginx 웹서버를 실행시키는 명령입니다.
CMD ["nginx", "-g", "daemon off;"]